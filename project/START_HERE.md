# START HERE

이 프로젝트를 처음 보거나, 세션이 끊겨서 컨텍스트가 없을 때 **이 파일부터**.

---

## 1. 뭘 하는 프로젝트인가

수백 대의 레이저 프로젝터가 공중에서 만나 허공에 그림을 만듭니다. 빔은 그림을
지나 **벽에 닿고, 거기에 그림의 선명한 복제본**을 그립니다. 그 벽에 붙일
패널을 설계하는 일입니다.

패널의 임무는 **세 가지이고 서로 다릅니다.**

| 축 | 무엇 | 왜 따로 재나 |
|---|---|---|
| **총 반사량** | 되돌아온 빛의 총합 | 적분구로 재는 양. 벽 전체가 얼마나 밝은지 |
| **형태 파괴** | 되돌아온 빛이 모양을 유지하나 | **1순위.** 글자가 읽히면 실패 |
| **정면 밝기** | 그 빛이 관측자 눈에 얼마나 들어오나 | 총량이 같아도 좁게 몰리면 눈부심 |

셋이 따로 놉니다. **벌집은 총 반사량을 5배 줄이면서 형태도 정면 밝기도 평판과
똑같습니다.** 하나만 재고 고르면 틀립니다.

## 2. 순서대로 읽을 것

1. **`principles/00_how_to_run.md`** — 명령·파일·채점 기준·순서. **이것만으로
   실험을 이어서 할 수 있어야 합니다**
2. `principles/01`~`06` — 각 원칙과, 그걸 어겨서 무엇을 잃었는지
3. `SAMPLES.md` — 지금 만들려는 시제품 (생성물, 손대지 말 것)
4. `results/QUESTIONS.md` — 미해결 질문 12건
5. `JOURNAL.md` — 과정과 틀린 길
6. `CONTEXT.md` — 긴 이력. **§11a 를 먼저** 읽으십시오

`metrics/` 는 지표 정의와 **각 지표의 알려진 결함**. `reference/` 는 논문 정독
노트.

## 3. 한 명령

```
cd project
./scripts/build_all.sh
```

게이트 → 순위 → 사양서 → 렌더 → 리포트 → 게이트. 한 단계라도 실패하면
아래를 안 만듭니다.

## 4. 지금 상태 (2026-08-13)

**측정 끝난 것**
- 어둡기: 설계 60 개 × 시드 13, `results/sweep_buildable.csv`
- 형태: 후보 11 개 × 16 위상 × 3 각도, `results/form_buildable.json`
- 거칠기 5 단계, `results/form_roughness.json`
- 상용 허니콤(셀 동일), `results/sweep_comb.csv`

**제조 가능한 것 중 순위** (평판 1.1413 % 대비)

| 설계 | 총 반사량 | 형태 | 정면 밝기 | 공정 |
|---|---|---|---|---|
| 날 배열 5.5/0.1 격자 | 0.2137 % (5.3배) | 3.44배 | 1.15 | 판금 끼움 |
| 콘 5.5/팁 0.4 | 0.2170 % (5.3배) | **4.11배** | **0.068** | 몰드 |
| 상용 허니콤 5.2/0.04 | 0.2092 % (5.5배) | 0.97배 | 1.64 | **구매** |

**총 반사량은 셋이 사실상 같습니다.** 갈리는 것은 나머지 두 축입니다.

## 5. 반드시 알고 시작할 것

- **실물 측정이 0 건입니다.** 전부 시뮬레이션입니다
- **코팅의 두 성질이 미측정**이고 그것이 절대값을 정합니다 — 확산 비율
  (시료 자신의 값이 1.06~1.39 배 변함), 거칠기 (콘의 정면 밝기가 25 배 변함).
  **평판 시료 하나를 고니오미터로 재면 둘 다 풀립니다**
- 이틀 동안 **결론 4 건이 발표됐다 철회**됐습니다. 넷 다 새 물리가 아니라
  설정 오류였습니다. `principles/01` 에 표로 있습니다
- **선행연구가 있습니다** — Kaster, *J. Appl. Phys.* 138, 174904 (2025),
  Carl Zeiss. 같은 문제, 시뮬레이션만. 그리고 US 11,209,577 B2 가 mm 급 경사
  매크로 구조를 규칙·불규칙 배열 모두 청구하고 있습니다

## 6. 하지 말 것

- `SAMPLES.md`, `report/*/report.html`, `report/*/data.json` 을 손으로 고치기
  — 생성물입니다. 다음 실행에 덮어써집니다
- 큐가 도는 중에 `run_queue.sh` 고치기 — zsh 가 루프를 한 번만 파싱합니다
- Blender 를 `--python-exit-code 77` 없이 돌리기 — 예외가 나도 0 을 반환합니다
- 순위를 냈는데 공정과 최소 피처를 같이 안 적기 — 설계가 아니라 상자를 순위
  매기는 것입니다
- 스윕을 돌린 뒤에 검증하기. **먼저 게이트, 그다음 스윕**
